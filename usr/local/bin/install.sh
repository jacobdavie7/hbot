#!/bin/bash

function install ()
{
    echo -e "\nRun full install and setup script? (yes or n)"
    read ANS
    if [ "$ANS" == "yes" ]; then
        echo -e "\nstuff"
        #updates
        #sudo group
        #apt installation
        #graphics
        #appearance
        #dns
        #firewall
    fi
}